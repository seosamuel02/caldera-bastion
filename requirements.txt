# BASTION Plugin Dependencies
# =================================
# These are additional dependencies required by the BASTION plugin.
# Core Caldera dependencies (aiohttp, pyyaml, etc.) are already installed.

# OpenSearch/Elasticsearch client for IntegrationEngine
# Used for querying Wazuh Indexer alerts
opensearch-py>=2.0.0

# Date/time parsing utilities
python-dateutil>=2.8.0

# Development dependencies (for testing)
# --------------------------------------
# pytest>=7.0.0
# pytest-asyncio>=0.23.0
# pytest-aiohttp>=1.0.0
# pytest-cov>=4.0.0
# coverage>=7.0.0
# flake8>=7.0.0
# pre-commit>=3.0.0
# tox>=4.0.0
